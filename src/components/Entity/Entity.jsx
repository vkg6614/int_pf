import React from "react";

const Entity = () => {
  return (
    <div>
      <button type="button" className="btnMargin">
        Add Entity
      </button>
    </div>
  );
};

export default Entity;
